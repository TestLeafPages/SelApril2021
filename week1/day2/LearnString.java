package week1.day2;

public class LearnString {

	public static void main(String[] args) {
		
		String str = "welcome";
		
		//System.out.println(str.replace('e', 'x'));
		
		System.out.println(str.replace("wel", ""));
		
		
		
		/*
		 * //declare using literal string String str1 = "welcome"; //each character has
		 * index, index start 0
		 * 
		 * 
		 * //method to find the number of characters in the string
		 * System.out.println(str1.length());
		 * 
		 * //to find the last character index System.out.println(str1.length()-1);
		 * 
		 * //to conver the string into character array char[] ch = str1.toCharArray();
		 * System.out.println(ch[3]);
		 */
		
		
		
		//method to get single character from String
		//System.out.println(str1.charAt(3));
		/*
		 * String revStr="";
		 * 
		 * for (int i = str1.length()-1; i >=0; i--) {
		 * 
		 * revStr = revStr+str1.charAt(i);
		 * 
		 * }
		 * 
		 * if (str1.equals(revStr)) {
		 * System.out.println("given string is a palindrome");
		 * 
		 * } else { System.out.println("given string is not palindrome"); }
		 */
		/*
		 * String str2 = "welcome";
		 * 
		 * System.out.println(str1.equals(str2)); //System.out.println(str1 == str2);
		 * 
		 * System.out.println(str1.equalsIgnoreCase(str2));
		 */
		
		
		
		
		/*
		 * //declare String using new keyword String str2 = new String("Welcome");
		 * 
		 * //to compare the address System.out.println(str == str1);
		 * System.out.println(str == str2);
		 * 
		 * System.out.println("********************"); //method to compare 2 string
		 * System.out.println(str.equals(str1)); System.out.println(str.equals(str2));
		 */
		

	}

}
